const docroiMaturityLabels = {
  1: "Ausente",
  2: "Debil",
  3: "Parcial",
  4: "Maduro",
  5: "Optimizado"
};

const docroiKaiQuestions = [
  { id: "strategy", title: "Alineacion estrategica", question: "Tu empresa toma decisiones basadas en objetivos realmente compartidos?", help: "Evalua si las areas trabajan con prioridades, metricas y criterios comunes.", paths: ["kai.phi"], lowRisk: "Las decisiones pueden generar mucha actividad, pero poco valor coordinado.", recommendation: "Crear un marco comun de objetivos, KPIs y prioridades ejecutivas." },
  { id: "decision", title: "Calidad de decision", question: "Las prioridades comerciales y operativas se deciden con criterios claros y repetibles?", help: "Mide si la organizacion decide por evidencia, foco y aprendizaje, no solo por urgencia.", paths: ["kai.usability"], lowRisk: "La ejecucion puede depender demasiado de criterio individual o presion de corto plazo.", recommendation: "Definir criterios de decision compartidos para priorizar inversion, clientes y acciones." },
  { id: "relationship", title: "Relacion activa con clientes", question: "La empresa mantiene una relacion activa y recurrente con sus clientes?", help: "Valora si la relacion continua despues de la venta y genera senales utiles para negocio.", paths: ["kai.networkIndex"], lowRisk: "Puede perderse recurrencia, recomendacion y visibilidad sobre oportunidades reales.", recommendation: "Activar ciclos de contacto, seguimiento y aprendizaje sobre comportamiento cliente." },
  { id: "intelligence", title: "Inteligencia de datos", question: "Los datos de clientes y negocio se convierten realmente en informacion util para decidir?", help: "Observa si el dato termina en decisiones, no solo en informes o cuadros de mando.", paths: ["kai.frequency", "kai.dataActivation"], lowRisk: "La organizacion puede tener datos, pero no convertirlos en decisiones economicas.", recommendation: "Conectar datos, decisiones y acciones comerciales con responsables y cadencia clara." },
  { id: "rfm", title: "Lectura de comportamiento cliente", question: "La empresa diferencia clientes segun frecuencia, valor y nivel de actividad?", help: "Permite saber que clientes son recientes, recurrentes, valiosos o en riesgo.", paths: ["kai.cc"], lowRisk: "Todos los clientes pueden recibir el mismo tratamiento aunque no tengan el mismo valor.", recommendation: "Segmentar cartera por comportamiento para priorizar retencion, crecimiento y reactivacion." },
  { id: "abcd", title: "Rentabilidad de oferta", question: "Tus productos o servicios estan clasificados segun rentabilidad e impacto real?", help: "Ayuda a saber que oferta crea margen, traccion, recurrencia o posicionamiento.", paths: ["kai.abcd"], lowRisk: "La empresa puede impulsar productos visibles pero no necesariamente rentables.", recommendation: "Ordenar la oferta por contribucion economica, potencial comercial e impacto estrategico." },
  { id: "nps", title: "Satisfaccion accionable", question: "La satisfaccion del cliente se mide y utiliza para mejorar decisiones?", help: "No se trata solo de medir satisfaccion, sino de usarla para corregir, priorizar y crecer.", paths: ["kai.nps"], lowRisk: "La voz del cliente puede llegar tarde o no influir en las decisiones clave.", recommendation: "Vincular satisfaccion, reclamaciones y recomendacion con decisiones de producto y servicio." },
  { id: "spo", title: "Modelo de priorizacion", question: "Existe una forma estructurada de priorizar clientes, oferta y acciones?", help: "Evalua si la empresa sabe donde actuar primero para proteger valor y capturar crecimiento.", paths: [], lowRisk: "Puede haber dispersion comercial: muchas acciones, poca prioridad y valor dificil de defender.", recommendation: "Crear una matriz ejecutiva de priorizacion entre cartera, oferta, esfuerzo y retorno." },
  { id: "productivity", title: "Productividad operativa", question: "La organizacion mide realmente productividad y eficiencia operativa?", help: "Mide si el crecimiento se acompana de eficiencia, capacidad y control del coste de operar.", paths: ["kai.purpose"], lowRisk: "El crecimiento puede aumentar complejidad y coste sin mejorar margen.", recommendation: "Unir objetivos comerciales con productividad, capacidad operativa y eficiencia por proceso." },
  { id: "portfolio", title: "Salud de cartera y contexto", question: "La empresa monitoriza la salud y evolucion de su cartera de clientes?", help: "Observa si se anticipan cambios de mercado, perdida de clientes, recurrencia y oportunidades.", paths: ["kai.context"], lowRisk: "La empresa puede reaccionar tarde ante deterioro de cartera, churn o cambios de mercado.", recommendation: "Implantar una lectura recurrente de cartera, riesgo, recurrencia, crecimiento y contexto." }
];

if (!state.kaiUx) state.kaiUx = {};
function docroiSetPath(path, value) { const keys = path.split("."); const last = keys.pop(); const target = keys.reduce((obj, key) => obj[key], state); target[last] = value; }
function docroiScoreFromPercent(value) { if (!hasInput(value)) return ""; const raw = n(value); if (raw >= 1 && raw <= 5) return Math.round(raw); return Math.min(5, Math.max(1, Math.round(raw / 20))); }
function docroiExecutiveScore(question) { if (hasInput(state.kaiUx?.[question.id])) return Number(state.kaiUx[question.id]); const firstPath = question.paths?.[0]; if (firstPath) return docroiScoreFromPercent(readPath(firstPath)); return ""; }
function docroiScoreToDecimal(score) { return Math.min(5, Math.max(1, n(score))) / 5; }
function docroiKaiScore(id) { const question = docroiKaiQuestions.find((item) => item.id === id); return docroiScoreToDecimal(state.kaiUx?.[id] || docroiExecutiveScore(question) || 0); }
function docroiSyncKaiUx() { docroiKaiQuestions.forEach((question) => { const score = docroiExecutiveScore(question); if (!hasInput(score)) return; state.kaiUx[question.id] = Number(score); question.paths.forEach((path) => docroiSetPath(path, Number(score) * 20)); }); }
function docroiMaturityText(score) { return docroiMaturityLabels[Math.round(n(score))] || "Pendiente"; }

calculateKai = function calculateKaiExecutiveLayer() {
  docroiSyncKaiUx();
  const answered = docroiKaiQuestions.filter((question) => hasInput(state.kaiUx?.[question.id])).length;
  const missing = docroiKaiQuestions.filter((question) => !hasInput(state.kaiUx?.[question.id])).map((question) => question.id);
  const cost = n(readPath("kai.cost"));
  if (missing.length || cost <= 0) return { incomplete: true, missing, psi: null, spo: null, kaiStar: null, ce: null, md: null, cost, answered };
  const psi = (docroiKaiScore("intelligence") + docroiKaiScore("relationship")) / 2;
  const ccOperational = (docroiKaiScore("rfm") + docroiKaiScore("spo")) / 2;
  const spo = ccOperational * docroiKaiScore("abcd") * docroiKaiScore("nps");
  const kaiStar = docroiKaiScore("strategy") * docroiKaiScore("decision") * docroiKaiScore("intelligence") * psi * spo * docroiKaiScore("productivity") * docroiKaiScore("portfolio");
  const md = n(readPath("kai.monetization"));
  const wacc = decimalFromPercentPath("meta.wacc");
  const ce = ((((kaiStar * md) - cost) / cost) - wacc) / Math.max(wacc, 0.0001);
  return { incomplete: false, psi, spo, kaiStar, md, cost, ce, enterpriseCe: ce, vCe: ce > 0 ? 100 : 0, answered };
};

function docroiRatingQuestion(question, index) { const value = docroiExecutiveScore(question); const buttons = [1, 2, 3, 4, 5].map((score) => `<button type="button" class="${Number(value) === score ? "selected" : ""}" data-kai-score="${question.id}" data-score="${score}"><strong>${score}</strong><span>${docroiMaturityLabels[score]}</span></button>`).join(""); return `<article class="exec-question"><div class="exec-question-head"><span>${String(index + 1).padStart(2, "0")}</span><div><h3>${question.title}</h3><p>${question.question}</p></div></div><p class="exec-help">${question.help}</p><div class="rating-row">${buttons}</div></article>`; }
function docroiExecutiveSummaryRows() { return docroiKaiQuestions.map((question) => { const score = docroiExecutiveScore(question); const interpretation = hasInput(score) ? Number(score) <= 2 ? question.lowRisk : Number(score) === 3 ? "Existe una base aprovechable, pero todavia no opera con suficiente consistencia ejecutiva." : "La organizacion muestra una capacidad solida para convertir esta dimension en valor." : "Pendiente de respuesta."; return `<tr><td><strong>${question.title}</strong></td><td>${hasInput(score) ? `${score}/5 - ${docroiMaturityText(score)}` : "Pendiente"}</td><td>${interpretation}</td><td>${question.recommendation}</td></tr>`; }).join(""); }

renderEquity = function renderEquityExecutiveConversation() { docroiSyncKaiUx(); const result = calculate().kai; const answered = docroiKaiQuestions.filter((question) => hasInput(state.kaiUx?.[question.id])).length; return `<div class="kai-intro executive-kai-intro"><div><p class="eyebrow">Customer Equity Activo</p><h3>Conversacion ejecutiva sobre valor cliente</h3><p>Responde con criterio de direccion. No es un examen tecnico: ayuda a entender como la organizacion convierte decisiones, datos, cartera y productividad en valor economico.</p></div><a href="https://docroi.marketing/kai-equation/" target="_blank" rel="noopener">Ver base conceptual</a></div><div class="plain-note executive-note"><strong>${answered}/10 dimensiones respondidas</strong><p>La estructura formal KAI·ROI v1 queda custodiada internamente. En esta pantalla solo trabajamos con lenguaje de negocio y madurez ejecutiva.</p></div><div class="exec-question-list">${docroiKaiQuestions.map(docroiRatingQuestion).join("")}</div><div class="explainer-grid kai-summary"><div class="explainer"><span>Madurez respondida</span><strong>${answered}/10</strong></div><div class="explainer"><span>Capacidad estructural</span><strong>${fmtKai(result.kaiStar)}</strong></div><div class="explainer"><span>Customer Equity</span><strong>${fmtKai(result.ce)}</strong></div></div>`; };

const docroiBindBeforeExecutiveKai = bindInputs;
bindInputs = function bindInputsExecutiveKai() { docroiBindBeforeExecutiveKai(); document.querySelectorAll("[data-kai-score]").forEach((button) => { button.addEventListener("click", () => { const question = docroiKaiQuestions.find((item) => item.id === button.dataset.kaiScore); if (!question) return; state.kaiUx[question.id] = Number(button.dataset.score); question.paths.forEach((path) => docroiSetPath(path, Number(button.dataset.score) * 20)); changed(true); }); }); };

renderKpiExplain = function renderKpiExplainExecutive(result) { const rows = [["ROI", pct(result.roi), "Retorno sobre la inversion total del proyecto."], ["VAN/VNA base", money(result.vanBase), "Valor actual neto antes de aplicar el ajuste K."], ["Valor K", `${num(result.kPercent)} %`, "Sensibilidad aplicada al VAN/VNA. Si escribes 0,7, el modelo aplica 0,7%."], ["VAN/VNA ajustado", money(result.van), "VAN/VNA base multiplicado por 1 + K."], ["Payback", result.payback ? `Mes ${result.payback}` : "No recuperado", "Mes en el que la caja acumulada compensa la inversion."], ["LTV diferencial", money(result.ltv.differential), "Valor extra por cliente al mejorar la retencion."], ["Customer Equity", fmtKai(result.kai.ce), result.kai.incomplete ? "Pendiente: completa la conversacion ejecutiva y la base economica del caso." : "Lectura del potencial de valor cliente ajustado por coste y exigencia financiera."]]; const combinedWarning = Number(state.kaiUx?.nps || 0) <= 2 && Number(state.kaiUx?.rfm || 0) <= 2 ? `<div class="plain-note"><strong>Lectura combinada relevante</strong><p>La organizacion tiene limitada visibilidad tanto de satisfaccion como de comportamiento cliente, lo que dificulta priorizar relaciones de valor y proteger recurrencia.</p></div>` : ""; return `<table class="result-table"><thead><tr><th>KPI</th><th>Resultado</th><th>Como leerlo</th></tr></thead><tbody>${rows.map((x) => `<tr><td><strong>${x[0]}</strong></td><td>${x[1]}</td><td>${x[2]}</td></tr>`).join("")}</tbody></table><div class="kai-report"><h3>Lectura ejecutiva de Customer Equity</h3><p>Esta lectura no evalua personas. Evalua madurez estructural, capacidad de decision, monetizacion del dato, alineacion operativa y potencial de valor cliente.</p>${combinedWarning}<table class="result-table"><thead><tr><th>Dimension</th><th>Resultado</th><th>Interpretacion</th><th>Recomendacion</th></tr></thead><tbody>${docroiExecutiveSummaryRows()}</tbody></table><p class="trace-note">Trazabilidad interna: la estructura formal KAI·ROI v1, titularidad de PhD Jorge Lucio, permanece intacta y subordinante sobre la implementacion operativa.</p></div>`; };

renderReport = function renderReportExecutiveOnly() { const result = calculate(); const decision = decisionText(result); document.getElementById("reportMeta").textContent = `${state.meta.project || "Proyecto sin nombre"} - ${state.meta.team || "Equipo"} - ${state.meta.date || "Fecha"}`; const monthlyRows = months.map((month, i) => `<tr><td>${month}</td><td>${money(result.monthlyRevenue[i])}</td><td>${money(result.monthlyOpex[i])}</td><td>${money(result.cashFlow[i])}</td><td>${money(result.accumulated[i])}</td></tr>`).join(""); document.getElementById("reportBody").innerHTML = `<section class="report-section"><h3>Lectura ejecutiva</h3><p><strong class="${decision.status}">${decision.title}.</strong> ${decision.body}</p><p>${state.notes || ""}</p></section><section class="report-section"><h3>Indicadores explicados</h3>${renderKpiExplain(result)}</section><section class="report-section"><h3>Resumen mensual</h3><table class="result-table"><thead><tr><th>Mes</th><th>Ingresos</th><th>OPEX</th><th>Cash flow</th><th>Acumulado</th></tr></thead><tbody>${monthlyRows}</tbody></table></section>`; };

updateStepRenderer(6, renderEquity);
renderCurrentStep();
renderLive();
renderReport();
